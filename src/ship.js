const generateMarkdown = (version, config) => {}

const updateChangelog = (content, config) => {}

const bumpVersion = (version, config) => {}

module.exports = { generateMarkdown, updateChangelog, bumpVersion }
